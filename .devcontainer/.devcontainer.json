{
  "name": "Laravel 12 Web Base",
  "image": "mcr.microsoft.com/devcontainers/php:8.3",
  "forwardPorts": [8000, 6379],
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y redis-server redis-tools",
  "postStartCommand": "redis-server --daemonize yes"
}
